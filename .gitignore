node_modules/
dist/
.astro/
.vercel/
*.log
.DS_Store

.playwright-mcp/
_preview.log
_*.cjs

# OG images — artefactos de build, generados por `npm run generate:og`
public/og/

# Descargas crudas del ETL (282MB, reproducibles desde la API CKAN de Corte Electoral) —
# la data procesada vive en public/data/. No bloquear el repo ni el build de Vercel con esto.
data/raw/

# Dimensión personas↔hoja (Epic 21): intermedios de build reproducibles desde CKAN
# (scripts/build-personas-*.py). El person×hoja crudo pesa ~59 MB (duplicación de nóminas
# de senadores × ~2800 hojas), fuera de escala para commitear/servir. El plan scopea el
# servido/API (21.3) aparte; acá solo se construye y verifica standalone. Cuando exista 21.3,
# el artefacto servido debe keyear persona→(elección, cargo, sublema/lema), NO person×hoja.
public/data/personas/

# Screenshots de verificación sueltos en la raíz (artefactos de debugging, no del producto).
/*.png
data/processed/geographic/_nacional/
data/processed/_annex_tmp/

# Intermedios de ETL: regenerables corriendo el pipeline desde data/raw/. No se versionan
# (antes estaban en Git LFS → agotaban la cuota y rompían CI + deploy de Vercel). El dato
# que SIRVE la app vive ya simplificado en public/data/**.
data/processed/

# Geo grande LEGACY de v1 en la raíz de public/ (NO lo sirve v2 — runtime usa
# public/data/geo/*.topo.json). Eran inputs/artefactos de ETL en LFS. Fuera del repo.
/public/*_map.json
/public/*_series_map.json
/public/uruguay_series_electorales.json
/public/uruguay_series_electorales_map.json
/public/montevideo_series_electorales.json
/public/v_sig_barrios.json
/public/regions.json
/public/elections-catalog.json
/public/partidos_abrev.json

# Borrador del post de LinkedIn (no es parte del producto)
POST_LINKEDIN.md

# Spikes / experimentos descartables (throwaway research code)
spikes/
etl/geometry/spike-geo-join.ts

# Código legacy archivado (v1, pre-overhaul). Se conserva en disco, fuera del repo.
legacy/

# Artefactos de trabajo de BMAD (working docs internos del método, no del producto)
_bmad/
docs/bmad-output/

# Python bytecode
scripts/__pycache__/
__pycache__/
*.pyc

# Brainstorm visual companion (superpowers)
.superpowers/
